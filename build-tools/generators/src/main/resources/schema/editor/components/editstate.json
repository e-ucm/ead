{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.editor.components.EditState",
    "extends": {
        "$ref": "../../engine/components/component.json"
    },
    "properties": {
        "view": {
            "type": "string",
            "description": "Fully qualified class of the current view"
        },
        "arguments": {
            "type": "array",
            "description": "Arguments of the view"
        },
        "sceneorder": {
            "items": {
                "type": "string"
            },
            "type": "array",
            "description": "Holds the list of scenes in the order they have to be shown in the editor. For each scene its id (e.g. scene0) is stored in this array. This element has no effect whatsoever in the actual order in which scenes are displayed in the game."
        }
    },
    "type": "object",
    "description": "A simple note for annotating stuff on editor schema."
}