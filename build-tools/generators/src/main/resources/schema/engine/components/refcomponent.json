{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.components.RefComponent",
    "type": "object",
    "extends": {
        "$ref": "component.json"
    },
    "description": "A component that just reference another component stored in an uri",
    "properties": {
        "uri": {
            "type": "string",
            "description": "Uri to the referenced component"
        }
    }
}