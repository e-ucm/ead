{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.components.behaviors.touches.Touch",
    "type": "object",
    "extends":{
        "$ref":"../../../data/condition.json"
    },
    "description": "Defines a touch interaction. Has a list of effects associated that only get queued for execution if the touch's condition is evaluated to true.",
    "properties": {
        "effects": {
            "type": "array",
            "items": {
                "$ref": "../../../effects/effect.json"
            },
            "description": "Effects launched win this interaction is activated."
        }
    }

}