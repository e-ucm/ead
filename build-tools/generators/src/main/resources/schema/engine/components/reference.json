{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.components.Reference",
    "type": "object",
    "extends": {
        "$ref": "component.json"
    },
    "properties": {
        "folder": {
          "type": "string",
          "description": "Points to the folder of the reference. Appended to the library. E.g. /myEntity"
        },
        "entity": {
            "type": "string",
            "description": "Points to the .json file of the element referenced. Appended to the folder path. E.g. /entity.json"
        }
    },
    "description": "A component that references another entity."
}