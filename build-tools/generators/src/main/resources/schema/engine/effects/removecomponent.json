{
    "description": "Removes a component from an entity",
    "additionalProperties": false,
    "extends": {
        "$ref": "effect.json"
    },
    "javaType": "es.eucm.ead.schema.effects.RemoveComponent",
    "properties": {
        "component": {
            "description": "The class alias of the component to be removed from the entity. If the class alias provided is not recognized, nothing happens.",
            "type": "string"
        }
    },
    "type": "object"
}