{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.effects.Effect",
    "type": "object",
    "description": "Effects define events that affects/changes the game state.",
    "extends": {
        "$ref": "../data/parameters.json"
    },
    "properties": {
        "target": {
            "type": "string",
            "default": "$_this",
            "description": "Expression that defines which entities this effect has to be applied to. The result of the expression must be an entity or a collection of entities."
        }
    }
}