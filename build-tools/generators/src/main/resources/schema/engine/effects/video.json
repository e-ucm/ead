{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.effects.Video",
    "type": "object",
    "extends": {
        "$ref": "effect.json"
    },
    "description": "This effect shows a video.",
    "properties": {
        "uri": {
            "type": "string"
        },
        "skippable": {
            "type": "boolean",
            "description": "If the video can be skipped."
        }
    }
}