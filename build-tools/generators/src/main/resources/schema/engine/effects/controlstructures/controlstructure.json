{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.effects.controlstructures.ControlStructure",
    "type": "object",
    "description": "Abstract definition of a flow control structure (if-then-else, if-then-else-if, while, foreach, etc.).",
    "extends": {
        "$ref": "../effect.json"
    },
    "properties": {
        "effects": {
            "type": "array",
            "items": {
                "$ref": "../effect.json"
            },
            "description": "The list of effects associated to this control structure."
        }
    }
}