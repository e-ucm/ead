{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.effects.controlstructures.While",
    "type": "object",
    "description": "Typical block while (condition){effects}.",
    "extends":{
        "$ref":"controlstructure.json"
    },
    "properties":{
        "condition":{
            "type":"string",
            "description": "A boolean expression. The list of effects is executed while this condition holds true"
        },
        "effects":{
            "type": "array",
            "items": {
                "$ref": "../effect.json"
            },
            "description": "A list of effects that are queued for execution while the condition is evaluated to true."
        }
    }
}

