{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.effects.controlstructures.If",
    "type": "object",
    "description": "Typical block if (condition){effects}.",
    "extends": {
        "$ref": "controlstructure.json"
    },
    "properties": {
        "condition": {
            "type": "string",
            "description": "A boolean expression. The list of effects is only executed if the condition is evaluated to true."
        }
    }
}