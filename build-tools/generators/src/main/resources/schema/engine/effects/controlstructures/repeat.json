{
    "additionalProperties": false,
    "javaType": "es.eucm.ead.schema.effects.controlstructures.Repeat",
    "type": "object",
    "description": "Equivalent to for (int i=0; i<n; i++){effects}",
    "extends": {
        "$ref": "controlstructure.json"
    },
    "properties": {
        "counter": {
            "type": "string",
            "description": "Identifies the name of the variable used for counter. Set to i by default.",
            "default": "i"
        },
        "times": {
            "type": "integer",
            "description": "the number of loops. The counter goes from 0 to times-1"
        }
    }
}