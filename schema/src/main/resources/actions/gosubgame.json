{
    "additionalProperties": false,
    "extends": {
        "$ref": "action.json"
    },
    "javaType": "es.eucm.ead.schema.actions.GoSubgame",
    "properties": {
        "name": {
            "description": "Name of the subgame. The engine will attempt to load a game stored in subgames/name",
            "type": "string"
        },
        "postactions": {
            "type": "array",
            "items": { "$ref": "action.json" },
            "description": "Actions to be executed after the game is ended through an 'end game' action"
        }
    },
    "type": "object"
}