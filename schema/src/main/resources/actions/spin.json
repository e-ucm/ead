{
    "additionalProperties": false,
    "extends": {
        "$ref": "temporalaction.json"
    },
    "javaType": "es.eucm.ead.schema.actions.Spin",
    "properties": {
        "forever": {
            "description": "Sets if it spins forever",
            "type": "boolean"
        },
        "spins": {
            "description": "Number of spins to perform in the action duration",
            "type": "integer"
        }
    },
    "type": "object"
}