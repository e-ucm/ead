{
    "additionalProperties": false,
    "extends": {
        "$ref": "temporalaction.json"
    },
    "javaType": "es.eucm.ead.schema.actions.Transform",
    "properties": {
        "loop": {
            "type": "boolean"
        },
        "relative": {
            "description": "Sets if the transformation must be performed relative to the actual transformation",
            "type": "boolean"
        },
        "transformation": {
            "$ref": "../components/transformation.json"
        }
    },
    "type": "object"
}