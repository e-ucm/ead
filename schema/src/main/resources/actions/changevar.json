{
    "additionalProperties": false,
    "extends": {
        "$ref": "action.json"
    },
    "javaType": "es.eucm.ead.schema.actions.ChangeVar",
    "properties": {
        "variable": {
           "type": "string",
           "description": "The name of the variable to change. If the variable does not exist, it is created before being set.",
           "required": "true"
        },

        "expression": {
           "$ref": "../components/expression.json",
           "required": "true"
        }
    },
    "type": "object"
}