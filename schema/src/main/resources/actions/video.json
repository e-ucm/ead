{
  "additionalProperties": false,
  "description": "This action shows a video.",
  "javaType": "es.eucm.ead.schema.actions.Video",
  "type": "object",
  "extends": {
    "$ref": "action.json"
  },
  "properties": {
    "uri": {
      "$ref": "../components/uri.json"
    },
    "skippable": {
      "type": "boolean",
      "description": "If the video can be skipped."
    }
  }
}