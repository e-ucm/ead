{
    "additionalProperties": false,
    "extends": {
        "$ref": "textstyle.json"
    },
    "javaType": "es.eucm.ead.schema.renderers.Text",
    "properties": {
        "text": {
            "type": "string"
        },
        "style": {
            "$ref":"../components/uri.json",
            "description": "this uri should point to a textstyle.json object. This allows (1) defining the style embedded within the text in a compact way, since text extends textstyle (good for single use styles); and (2) defining the style in a separate file, that can be user-defined or system-defined, which allows the creator of the game specify consistent text styles for his/her games"
        }
    },
    "type": "object"
}